/**
 * Options for columns that can define a length of the column type.
 * Note: width, zerofill, and unsigned options were removed as they were MySQL-only features.
 */
export interface ColumnWithWidthOptions {}
